package LaiOffer.Class10_BitOperation;

/**
 * Bit Operation Basic Question 5: determine whether a word contains all letters that are unique
 */
public class BitOperation_ContainUniqueLetter {

    /**
     * Solution
     * Using int bit_map[8] to present 256 ascii character.
     * Since each integer can represent 32 bit, once a character occur, we change one bit in the int array into 1.
     * If we find a time that a bit of an integer has already become 1, then return false, otherwise keep iteration.
     */

}
