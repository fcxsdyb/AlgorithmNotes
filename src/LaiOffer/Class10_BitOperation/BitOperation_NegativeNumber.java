package LaiOffer.Class10_BitOperation;

public class BitOperation_NegativeNumber {

    /**
     * Principle: When we assign a negative number to an unsigned integer, the binary representation does NOT change.
     * However, we will calculate the value of the unsigned integer by using the current binary representation.
     *
     * int a = -1
     * unsigned int b = a // what is b? assuming 32 bits, value is 2^32 - 1
     */

}
