package LaiOffer.Class4_BinaryTree_BinarySearchTree;

public class BinarySearchTree {

    /**
     * Definition:
     * Binary Search Tree: for every single node in the tree,
     * the values in its left subtree are all smaller than its value,
     * and the values in its right subtree are all larger than its value.
     */

}
