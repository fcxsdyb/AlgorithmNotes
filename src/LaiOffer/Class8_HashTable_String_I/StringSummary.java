package LaiOffer.Class8_HashTable_String_I;

public class StringSummary {

    /**
     * String Common Questions:
     * 1. Removal
     *      1.1 remove some particular chars from a string
     *      1.2 remove all leading/trailing/duplicated empty spaces from a string
     * 2. De-duplication    aaaabbbb_ccccc -> ab_c
     * 3. Replace empty space    "_" with "20%"
     * 4. Reversal(swap)    I love yahoo -> yahoo love I
     * 5. Substring -> strstr
     *      5.1 regular method
     *      5.2 Robin-Carp(hash based string matching) & KMP(Knuth-Morris-Pratt)
     *
     * Advanced operations (Defer to String II)
     * 1. Move letters around   e.g. ABCD1234 -> A1B2C3D4
     * 2. Permutation (use DFS)
     * 3. Decoding/encoding     e.g. aaaabcc -> a4b1c2
     * 4. Longest substring that contains only unique chars
     * 5. Matching (*, ?)
     * 6. etc.
     *
     * Popular representations of characters:
     * 1. ASCII representation of a letter: A = 65, a = 97
     * 2. Unicode: The latest version of Unicode contains a repertoire of more than 110,000 characters
     * covering 100 scripts and various symbols.
     */

}
